interface:
  display_name: "BOOTH"
  short_description: "Read BOOTH seller products, orders, sales, and messages"
  default_prompt: "Use $booth to summarize my BOOTH shop and recent sales."

dependencies:
  tools:
    - type: "mcp"
      value: "booth"
      description: "Bundled read-only BOOTH seller MCP server"
      transport: "stdio"

policy:
  allow_implicit_invocation: true
